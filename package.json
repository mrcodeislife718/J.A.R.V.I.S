{
  "name": "jarvis-governed-core",
  "version": "0.7.0",
  "private": true,
  "description": "J.A.R.V.I.S governed AI operating core",
  "type": "module",
  "engines": {
    "node": ">=20.20.2"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "migrate": "tsx src/storage/migrate.ts",
    "infra:agent": "tsx src/infrastructure/node-agent.ts --watch",
    "infra:agent:once": "tsx src/infrastructure/node-agent.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "fastify": "5.10.0",
    "pg": "8.16.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "8.15.5",
    "tsx": "4.23.1",
    "typescript": "7.0.2"
  }
}
